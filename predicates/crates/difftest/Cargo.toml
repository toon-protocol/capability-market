[package]
name = "difftest"
version = "0.1.0"
edition = "2021"
description = "Differential-testing divergence predicate for the TOON capability market (toon-meta#122)"
license = "MIT"

[dependencies]
# Deliberately dependency-light: only the shared journal envelope crate. RISC
# Zero guest deps arrive with the toolchain wiring; see README.
journal = { path = "../journal" }
