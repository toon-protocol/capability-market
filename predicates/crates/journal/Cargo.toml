[package]
name = "journal"
version = "0.1.0"
edition = "2021"
description = "Canonical capability-market journal struct + byte encoding (toon-meta#121)"
license = "MIT"

[dependencies]
sha2 = { version = "0.10", default-features = false }

[dev-dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
hex = "0.4"
