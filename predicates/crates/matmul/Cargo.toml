[package]
name = "matmul"
version = "0.1.0"
edition = "2021"
description = "Flagship predicate: rank-bounded bilinear scheme verifier for 4x4 matmul over GF(2) (toon-meta#122)"
license = "MIT"

[dependencies]
journal = { path = "../journal" }
manifest = { path = "../manifest" }

[dev-dependencies]
matmul-methods = { path = "methods" }
risc0-zkvm = "3.0"
hex = "0.4"
