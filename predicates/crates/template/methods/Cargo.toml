[package]
name = "template-methods"
version = "0.1.0"
edition = "2021"
description = "risc0-build embedding of the template guest (ELF + image ID)"
license = "MIT"

[build-dependencies]
risc0-build = "3.0"

[package.metadata.risc0]
methods = ["guest"]
